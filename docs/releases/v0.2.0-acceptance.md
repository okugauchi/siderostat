# siderostat v0.2.0 acceptance

## Status

Status: **RELEASE BLOCKED / CANDIDATE ARTIFACT BUILT**

`1d3bda3^..86b2ac6` の実装、reconnect 自動試験、Thunderbolt 直結 2 node の実機 acceptance は
PASS している。3 package の version を `0.2.0` へ更新し、署名検証済み runtime/monitor と archive の
checksum を確定した。一方、artifact は未コミット変更を含む作業ツリーから生成されており、
`v0.2.0` tag と final artifact による upgrade/rollback rehearsal は未完了である。このため、
本書は最終 release acceptance をまだ PASS としない。

加えて 2026-08-18 の 2 node install 後、deployment manifest の互換 binary digest 集合が不一致の
場合に periodic reconcile が pairing block を迂回し、standalone child を反復再起動する問題を実機で
確認した。修正 commit `53aaa9c` は `fix/deployment-mismatch-restart-loop` にあり、現在の release
candidate artifact には含まれない。修正版の merge、clean build、両 node mismatch acceptance を
完了するまで release blocker とする。

## Release identity

| Item | Result | Evidence |
|---|---|---|
| Source range | PASS | `1d3bda3a31a149adf21395aece4138c08f007577` から `86b2ac6c539c91cf402bde8e157cc189c9127895` までを inclusive に監査 |
| Release merge | PASS | `449938000a23d5c30e6da76c45b57ed98f461b47` (`Merge develop into main for v0.2`) |
| Annotated release tag | PARTIAL | `v0.2` は release merge を指すが、SemVer 名 `v0.2.0` ではない |
| Cargo package version | PASS | `siderostat`、`siderostat-monitor`、`xtask` はすべて `0.2.0` |
| Release candidate binary checksums | PASS | runtime `a0b2f297…79b5`、monitor `fb756bc9…b73f` |
| Release candidate archive checksum | PASS | `siderostat-v0.2.0-aarch64-apple-darwin.tar.gz`: `ee25be66…b913` |
| Build source identity | PARTIAL | HEAD `86b2ac6` 上の作業ツリー。未コミット変更を含む |
| Deployment-mismatch restart-loop fix | FAIL | `53aaa9c` は fix branch 上で、candidate artifact に未収録 |

既存の reconnect candidate binary SHA-256 は実機 scenario の再現性を示す別 evidence であり、今回の
v0.2.0 release candidate checksum ではない。両者を release artifact 表で混同してはならない。

### Release candidate artifact

| Item | Value |
|---|---|
| Build date | `2026-08-18` |
| Target | `aarch64-apple-darwin` |
| Toolchain | `rustc 1.97.1 (8bab26f4f 2026-07-14)` |
| Runtime SHA-256 | `a0b2f29781e966b59b65a6d2f1a92ad7e1eb5fcdd642906d6a2c0f15188579b5` |
| Monitor SHA-256 | `fb756bc982c3592380fdbfc3014f181b81ab37e13906dd2c9da84f8471b9b73f` |
| Archive | `siderostat-v0.2.0-aarch64-apple-darwin.tar.gz` |
| Archive SHA-256 | `ee25be669cda88a9df564aec1ddc2bd6ea87bac4d65c06b0452ab6c7b654b913` |
| Signing | runtime/monitor を ad-hoc 再署名し、archive 作成前後に `codesign --verify` PASS |

Archive には `README.md`、runtime、monitor、example config、LaunchAgent README、runtime/monitor plist
だけを含める。model、GGUF、secret、実 config、state、cache、log がないことを listing で確認した。

## Reconnect acceptance mapping

### PeerLost lifecycle and serving consistency

| Criterion | Result | Evidence |
|---|---|---|
| peer loss 後に両 node が Solo Standalone serving | PASS | H-02/H-03/H-04、doctor healthy、admission serving、standalone readiness |
| distributed child を identity 確認付きで停止 | PASS | single-owner `PeerLossRecovery`、owned child cleanup、実機 process tree |
| stale child / orphan transition を残さない | PASS | 自動 race/failure suite、実機最終 checkpoint で DS4 child 各 node 最大 1 |
| state、target、admission、child identity が一致 | PASS | `/cluster`、`cluster status`、`cluster doctor`、public `/v1/models` HTTP 200 |
| recovery 途中の失敗で Ready を偽装しない | PASS | stop/start failure test、`SoloStandaloneStarting + Unavailable` 維持と retry |

### Control session and pairing

| Criterion | Result | Evidence |
|---|---|---|
| worker 高世代 / coordinator 高世代の両方向で収束 | PASS | Pair offer/confirm direction matrix、片側 restart tests |
| cluster/control generation を分離して永続化 | PASS | state round-trip、旧 fixture normalize、H-04 reboot acceptance |
| duplicate/遅延 Pair が冪等に収束 | PASS | delay/duplicate production test、session generation 非減少 |
| stale command と generation mismatch を拒否 | PASS | HTTP 409 mapping、expected/received structured log、stale tests |
| Pair timing race がない | PASS | 100/100 session、timeout 0、pair error 0、confirm-after-stability 0 |

### Network gate and failure recovery

| Criterion | Result | Evidence |
|---|---|---|
| route scope を production の実測 evidence から取得 | PASS | `NetworkEvidence`、固定 `route_scoped=true` の撤去 |
| 古い observation epoch を拒否 | PASS | stale snapshot/candidate tests |
| Bonjour 単独では peer present にしない | PASS | truth table、static fallback + authentication test |
| invalid route/address/interface で fail closed | PASS | 7 scenario matrix、Solo serving 維持 |
| Backoff deadline 後に自動復帰 | PASS | deadline 前後の periodic reconcile test |
| peer loss を Backoff より優先 | PASS | peer-loss-during-backoff test |
| operator reconcile が tracker reset と state 解除を原子的に行う | PASS | runtime/admin HTTP tests、promotion failure E2E |
| deployment mismatch 後に自動 pairing と child restart が停止 | FAIL | 実機で restart loop を確認。`53aaa9c` の merge と再 acceptance が必要 |

## Automated verification

| Gate | Result | Evidence |
|---|---|---|
| `cargo fmt --check` | PASS | reconnect Phase F final gate |
| Clippy all targets/all features, warnings denied | PASS | reconnect Phase F final gate |
| Default workspace tests | PASS | core 191、monitor 23、xtask 5、全 integration GREEN |
| `test-support` workspace tests | PASS | core 191、monitor 23、xtask 5、production reconnect 36、全 integration GREEN |
| 10-cycle Paired cable blip | PASS | `paired_cable_blip_repairs_to_paired_over_10_cycles` |
| 10-cycle Distributed cable blip | PASS | `distributed_cable_blip_rebuilds_new_generation_over_10_cycles` |
| Network truth table / detach-attach endurance | PASS | N-03 matrix、route detach/attach 10 cycle |
| Pair timing | PASS | 100 session 全収束 |
| Release build | PASS | 3 package を release profile で `0.2.0` として build |
| Binary architecture/signature | PASS | runtime/monitor は arm64 Mach-O、ad-hoc signature 検証済み |
| Archive content/checksum | PASS | 許可した 7 files のみ。binary/archive checksum を記録 |

## Two-node field acceptance

| Scenario | Result | Final behavior |
|---|---|---|
| H-01 candidate / rollback preparation | PASS | 同一 candidate、両 node Solo ready、backup と checksum を保全 |
| H-02 physical cable detach/reconnect ×2 | PASS | 各 cycle で Solo → Paired → DistributedReady、orphan なし |
| H-03 coordinator-only process restart ×2 | PASS | 自動 recovery / re-pair / promotion、新 child generation |
| H-03 worker-only process restart ×2 | PASS | 高世代方向を含め DistributedReady、409 loop なし |
| H-04 coordinator-only macOS restart | PASS | 約 3 分で両 node DistributedReady、API 200 |
| H-04 worker-only macOS restart | PASS | 約 2 分で両 node DistributedReady、API 200 |
| H-04 both macOS restart | PASS | 一時 503 後、約 3 分で両 node DistributedReady |

H-03 の初回試行では、SIGKILL policy と process identity の不整合による 409 loop を検出して停止した。
`05220cb` で recovery と owned child cleanup を修正し、同じ方向別 scenario を再実行して PASS した。
初回 failure は削除せず、修正後の成功と分けて acceptance record に保持している。

## macOS feature acceptance

| Feature | Result | Evidence / remaining work |
|---|---|---|
| Desktop notification state mapping | PASS | fake notifier、session availability、throttle、startup cleanup の deterministic tests |
| Notification failure isolation | PASS | 非 Aqua / 投稿失敗を warn のみにし、cluster state を変更しない |
| Menu bar monitor parser/state | PASS | core metrics、monitor 23 tests、offline fallback |
| AppKit main-loop integration | PASS | independent monitor process と tray menu 実装、workspace CI |
| Prefill / KV cache / Decode metrics | PASS | DS4 log parser、metrics export、完了時 stale value clear |
| Runtime/monitor LaunchAgent controls | PASS | restart / bootout menu と install wiring |
| Real DS4 Prefill display | PENDING | monitor spec Phase 5。実 DS4 の Prefill 中 UI 表示 evidence が未採取 |
| macOS Application Bundle / `.pkg` | NOT IN SCOPE | 現行配布は binary + LaunchAgent |

## Installation and operational acceptance

| Gate | Result | Evidence / note |
|---|---|---|
| `cargo xtask install/verify/uninstall` implementation | PASS | build、codesign、config/manifest/secret/plist、LaunchAgent 操作を実装 |
| Idempotent install and digest cache | PASS | secret 非上書き、model fingerprint の分離、xtask 5 tests |
| Extensionless secret naming | PASS | 標準名を更新し、legacy `.key` を migration 入力として許可 |
| Startup owned-process cleanup | PASS | H-01/H-03、identity 再確認、unknown process への signal 禁止 |
| Deployment mismatch convergence | FAIL | Solo recovery 後に periodic pairing が再開する実機不具合。`53aaa9c` で修正、未収録 |
| Independent v0.2.0 upgrade/rollback rehearsal | PENDING | reconnect 検証では rollback 資産を保全したが、v0.2.0 artifact の upgrade→rollback→re-upgrade は未実施 |

## Finalization gates

最終 status を `PASS` へ変更する前に、次を完了する。

- [x] root、monitor、xtask の package version を `0.2.0` へ更新する。
- [ ] tag 名を `v0.2.0` に統一する方針を決め、既存 `v0.2` との関係を release record に残す。
- [x] 現在の作業ツリーから runtime と monitor の release candidate artifact を生成する。
- [x] artifact 内容を監査し、model、secret、実 config、state、cache、log を含めない。
- [x] binary/archive の SHA-256、target、toolchain、DS4 baseline を記録する。
- [ ] version と monitor 変更を commit し、clean な final commit/tag から artifact を再生成する。
- [ ] `53aaa9c` 相当の deployment-mismatch restart-loop 修正を release ancestry へ取り込む。
- [ ] 不一致 manifest の 2 node 実機試験で、通知が一度だけ表示され、generation と child PID が安定することを確認する。
- [ ] final artifact で install/upgrade/rollback/verify を実施する。
- [ ] monitor を production feature として扱う場合は、実 DS4 の Prefill/KV/Decode 表示を確認する。
- [ ] Required CI と document link/checksum gate を final commit/tag に対して再実行する。

Reconnect の詳細な実機判定は
[`reconnect-acceptance-2026-08-17.md`](../compatibility/reconnect-acceptance-2026-08-17.md)、設計判断と
task evidence は [`reconnect-2026-08`](../archive/reconnect-2026-08/README.md)、release 概要は
[`v0.2.0.md`](v0.2.0.md) を参照する。
