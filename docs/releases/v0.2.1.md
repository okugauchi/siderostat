# siderostat v0.2.1

Status: **release candidate built / release blocked**

## Release scope

本releaseはcommit `1d3bda3`から`259b575`までをinclusiveに扱う。`259b575`を実装基準とし、
package version、process reaping、LaunchAgent restart待ち、release文書を含む候補とする。

| Item | Value |
|---|---|
| Source range | `1d3bda3^..259b575` |
| Implementation baseline | `259b575c2b046bc47859bc84f0b6242ea72e02a4` |
| Range size | 103 files、23,619 insertions、3,278 deletions、112 commits |
| Existing annotated tag | `v0.2` |
| Package version | `siderostat`、`siderostat-monitor`、`xtask`ともに`0.2.1` |
| Verified DS4 source baseline | `b0309611041655f4e45671cfd9c9886aff161406` |

`v0.2.0`のrelease文書は候補状態のまま旧blockerを含んでいたため、確定releaseとして残さず
`v0.2.1.md`と`v0.2.1-acceptance.md`へ改名した。既存の`v0.2` tagは変更しない。
`v0.2.1` tagはrelease blocker解消後にannotated tagとして作成する。

## Changes since the previous candidate

- deployment manifest不一致後にperiodic reconcileがpairing blockを迂回する問題を`53aaa9c`で修正し、
  merge commit `ef4c274`を経てrelease ancestryへ収録した。
- 不一致時は両nodeをSolo Standaloneへ収束させ、通知とfailure contextを保持したまま自動pairingと
  child restartを抑止する。
- GGUF digest cacheへ最大4 MiBの分散サンプル署名を追加した。size不変のmetadata driftはsample一致時に
  full SHA-256を再計算せずcache metadataを更新する。
- sampleを持たない旧cacheはmetadata完全一致時に自動upgradeする。metadata driftがある旧cacheだけ
  `--accept-model-metadata-change`による明示的なoperator確認を要求する。
- size変更またはsample不一致はoperator overrideを許可せず、full fingerprintを必須とする。
- identity確認済みowned DS4 childをSIGTERM後に確実にreapできるようにし、生成configの
  `allow_sigkill=true`と組み合わせて片側restartから自動復旧する。
- `cargo xtask install --start`は既存LaunchAgentの`bootout`完了を待ってから`bootstrap`し、
  status 5となる再install raceを回避する。
- Hermesの1,800秒stale timeoutよりproxy側が先に切断しないよう、install生成configの
  `first_body_byte`と`stream_idle`の既定値を2,400秒へ延長する。

## Validation summary

2026-08-18のall-feature再試験ではproduction reconnect 37件がPASSし、10-cycle
Paired/Distributed cable blip、100-session Pair timing、deployment mismatch latchを含む。
Thunderbolt直結2 nodeへ`259b575`をinstallした実機試験では、両nodeが`distributed-ready`へ収束し、
`cargo xtask verify`、doctor、public `/v1/models`がPASSした。不一致manifest試験でも、不一致検出から
手動停止まで再pairingとchild restartが発生せず、旧release blockerの修正を確認した。

`cargo xtask install`では旧digest cacheへ4つのsample署名が追加され、約156 GBのMXFP4を含むGGUF
full SHA-256の再計算を行わずに完了した。Thunderbolt 5 cableのdetach/reconnectも2 cycle連続で
検証し、各cycleでSolo servingへ安全に復帰後、再接続から約1分で新しいchildのDistributedReadyへ
自動収束した。

旧候補で判明した`allow_sigkill=false`の停止不能は修正済みである。`259b575`を両nodeへ再install後、
H-03 coordinator-only 2回、worker-only 2回を実施し、全cycleが新しいchild PID/generationの
DistributedReadyへ自動収束した。409、SIGKILL拒否、stale/orphan childは再発していない。

H-04はcoordinator-only、worker-only、両node同時macOS restartの全3 scenarioがPASSした。
各scenarioは手動復旧なしで約2分28秒から3分35秒以内に新しいdistributed childへ収束し、
最終doctor、healthz、readyz、public `/v1/models`は両nodeで正常だった。

Coordinator-onlyではworker childの停止完了まで約180秒かかり、その間Solo/Paired遷移が有限回反復した。
最終収束と安全性はPASSだが、対応する「Standalone起動」「ノード検出」通知が反復し得るため、
release前にUX risk acceptanceまたは通知抑制方針を決定する。

## Automated validation

- `cargo fmt --all -- --check`: PASS
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: PASS
- default workspace tests: core 193、monitor 23、xtask 13、integration tests PASS
- all-feature workspace tests: production reconnect 37を含めPASS
- `cargo build --release --workspace`: PASS
- runtime/monitor: arm64 Mach-O、ad-hoc codesignとarchive展開後の再検証PASS

## Release candidate artifact

Build date: 2026-08-18

Build sourceはcleanな`259b575`である。Checksumはcurrent-commit candidate evidenceであり、
annotated final tagからの再現artifactではない。

| Artifact | Size | SHA-256 |
|---|---:|---|
| `siderostat` | 12,380,256 bytes | `4a22fb89c8099a3c56f98a0cc71df929e0e46bb6be8daaa600f9fa0fc85dab79` |
| `siderostat-monitor` | 7,707,520 bytes | `4fca5a3361f805267604fac84376fbba5fa527d6264599e266131ed644284dd9` |
| `siderostat-v0.2.1-aarch64-apple-darwin.tar.gz` | 7,644,303 bytes | `0683e4ee0a4b1f3752be87627f868631c199076185f4c2059caa9db2d7cf3cd3` |

Candidate artifactは`/private/tmp/siderostat-v0.2.1-259b575.LjkRS0`に生成した。archiveにはREADME、runtime、
monitor、example config、LaunchAgent README、runtime/monitor plistの7 filesだけを含み、model、secret、
実config、state、cache、logを含まない。収録したexampleの`allow_sigkill=true`も監査した。

このcandidate archiveはproxy timeout既定値の変更前に生成されており、収録exampleの
`first_body_byte`と`stream_idle`は1200秒である。Annotated final tagからの再生成では2400秒の
exampleを収録し、archive checksumと内容監査を更新する。

## Known limitations and release blockers

- **GATE:** coordinator restart中の有限通知反復をrisk acceptanceまたは改善する。
- **GATE:** 2400秒のproxy timeout既定値を含むannotated final tagからのartifact再生成とchecksum確定。
- upgrade/rollback rehearsalは未実施。
- 実DS4 Prefill中のmonitor UI表示evidenceは未採取。
- macOS `.app` / `.pkg`、RDMA over Thunderbolt、Tensor Parallelは対象外。

詳細な判定は[`v0.2.1-acceptance.md`](v0.2.1-acceptance.md)を参照する。
